# Environment: local
# No secrets should be committed. Copy to a real env file and inject at runtime.

ENVIRONMENT=local

# Logging
LOG_LEVEL=INFO

# Optional: request correlation / tracing (populated by upstream proxy or middleware when available)
TRACE_ID=

# Postgres (local)
DATABASE_URL=postgresql+asyncpg://payscope:payscope@localhost:5432/payscope
DATABASE_DSN=postgresql://payscope:payscope@localhost:5432/payscope

# MinIO (S3 compatible)
S3_ENDPOINT_URL=http://localhost:9000
S3_REGION=us-east-1
S3_ACCESS_KEY_ID=payscope
S3_SECRET_ACCESS_KEY=payscope-secret
S3_BUCKET=payscope-raw

# Redis / Celery
CELERY_BROKER_URL=redis://localhost:6379/0

# Optional: HF model cache
HF_HOME=

# LLM mapping (OpenAI-compatible)
LLM_BASE_URL=
LLM_API_KEY=
LLM_MODEL=gpt-4o-mini
LLM_TIMEOUT_S=30
MAPPING_CONFIDENCE_THRESHOLD=0.70

# Free LLM Options (OpenAI-compatible, no paid API key required)
# Option 1: Hugging Face Inference API (free tier)
HF_API_TOKEN=
HF_MODEL=meta-llama/Llama-3.1-8B-Instruct
# Option 2: Local self-hosted (Ollama or vLLM)
LLM_PROVIDER=auto
LLM_LOCAL_PROVIDER=ollama
LLM_LOCAL_BASE_URL=
LLM_LOCAL_MODEL=

# Pinecone (vector DB)
PINECONE_API_KEY=
PINECONE_INDEX_NAME=
PINECONE_NAMESPACE=payscope

# Neo4j (graph DB)
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=


